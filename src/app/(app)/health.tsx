import { H1 } from "@/shared/ui";
import { View } from "lucide-react-native";

export default function Health() {
  return (
    <View className="flex h-screen items-center justify-center">
      <H1 className="text-4xl font-bold">Health Page</H1>
    </View>
  );
}