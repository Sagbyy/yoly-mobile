import type { HomeUser } from "./types";

// Static placeholder until wired to the authenticated user / API.
export const homeUser: HomeUser = {
  initials: "SR",
  greetingName: "Sarah",
  hasNotifications: true,
};
